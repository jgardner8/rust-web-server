[package]
name = "http_server"
version = "0.1.0"
edition = "2024"

[profile.release]
debug = true

[dependencies]
assertables = "9.9.0"
derive_try_from = { version = "0.1.0", path = "derive_try_from" }
urlencoding = "2.1.3"
